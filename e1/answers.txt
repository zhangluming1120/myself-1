1, np and pd both have same library, for example sum function: np.sum(axis = 0/1), pd.sum(axis = 0/1)
NumPy is easier to use, however; Pandas is harder to use but easier to read
2 pivot_months_pandas uses c libary which is faster than python.
